{
  "name": "pi-pitaj",
  "version": "0.1.1",
  "private": true,
  "description": "Ultra-fast in-process model consult extension for Pi",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test '*.test.ts'"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
