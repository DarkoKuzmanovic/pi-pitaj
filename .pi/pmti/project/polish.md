# PMTI polish backlog

Small improvements that should not distract from the current milestone unless they become acceptance criteria.

- Add concise troubleshooting for “pitaj overused” and “pitaj had too little context.”
- Make `/pitaj help` show snapshot/advisor examples after M1 exists.
- Add `/pitaj check` output that distinguishes missing alias, missing registration, and missing API key in a compact table.
- Consider result details that show snapshot provenance and omitted context categories.
- Consider a compact `/pitaj config` summary view before adding a full interactive settings editor.
- Consider examples comparing explicit `gpt`, explicit `opus`, `model: "auto"`, and curated snapshot usage once M1 is complete.
